/**
 * @author bernat
 */

/**Definition of the easyDAS base object*/

var easyDAS  = easyDAS || {};

(function() {
	//Private Attributes
	
	
	//Private Methods
	
	
	//Public Attributes
		
		
		
	//Public Methods
			
		//Used as described by Douglas Crockford to create real prototyping.
		//var newobj = easyDAS.object(oldobj) creates new object using oldobject as its prototype.
		/*easyDAS.object = function(o){
			function F(){
			}
			F.prototype = o;
			return new F();
		};*/
		
		//adds a throbber and the given text as the only content of the element
		easyDAS.markAsWaiting = function(el, text) { //TODO: Fix the size of the replaced element to avoid flashing size changes
			$(el).html('<div class="waiting"><img alt="waiting indicator" src="images/wait.gif" /><span class="label">'+text+'</span></div>');
		};
	
	
	

})();

 
